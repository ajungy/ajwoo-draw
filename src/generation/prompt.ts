/** Shared by clipboard handoff and provider requests so both preserve the drawing's intent. */
export function referencePrompt(prompt: string): string {
  return `Create a high-quality, polished image that matches the attached drawing as closely as possible. Treat the drawing as the primary reference for what to produce. Preserve its subject matter, layout, proportions, framing, colors, visible text, and arrangement of elements. Improve clarity, detail, and finish without changing the design or inventing a scene. If the drawing is a user interface, output only that interface, preserving its screens, controls, and labels; do not add a device mockup or surrounding environment. If it depicts a person or object, output only that subject with the same pose and composition; do not add unrelated subjects or scenery. Use the user's instructions below to refine the result, changing the reference only where explicitly requested. Return only the finished image.${prompt.trim() ? `\n\nUser instructions: ${prompt.trim()}` : ''}`;
}
